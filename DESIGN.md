# 自选股技术指标监测系统 — 评审 + 落地方案

> 评审对象:《自选股技术指标实时监测系统 规则规范 v2.0》
> 仓库名(已定):**`feifei_Ashare`** ,建议路径 `C:\Users\Administrator\feifei_Ashare`
> 定位:**运行时完全独立新仓**(零跨仓依赖),数据上**只读复用** China_quant 本地库;开发上 **fork(复制)quant-pipeline 已验证模块**,不 import
> 运行模式(已定 2026-06-27):**纯盘后分析**——每交易日收盘后(15:05+)批算一次,**不做盘中/实时**
> 本轮交付:批判性评审 + 分阶段落地方案(不写代码)
> 时间:2026-06-27 周六(休市)

---

## 0. 一句话结论

规范本身**水平在线**——多指标共振、震荡指标只看背离、大盘过滤当最高优先级、止损止盈对称、反对未来函数,这些都踩在了真东西上。**真正的工程量不在指标,在三处:① 筹码分布估算(最大不确定性源)、② 一个诚实的事件驱动回测器(最易被偷工)、③ 一套统一的信号状态机(最易出 bug)。** 指标本身用 pandas-ta 几小时就出来。

**最重要的一件事(你点了"独立新仓"前未必完全意识到):你现有的 `quant-pipeline` 已经实现了这套规范约 1/3 到 1/2 的内容。** 见第 1 节对照表。独立没问题,但建议**架构上沿用它的积木哲学 + 直接抄它的告警/数据 topup 模式**,否则是在重造已验证过的轮子。

---

## 1. 规范模块 × quant-pipeline 现状(决策关键)

| 规范模块 | quant-pipeline 现状 | 新仓工作量 |
|---|---|---|
| 数据层(日线/前复权/交易日历/四源可插拔/topup) | ✅ 已有且已验证(sources + `_topup_local`) | 只读复用 DB,取数代码可**直接抄** |
| MA/MACD/KDJ/RSI/ATR/BOLL 全套衍生 | 🟡 部分(factors 有 rsi/ma_ratio/momentum;无完整 MACD/KDJ/ATR/BOLL 衍生字段) | **自建指标层**(中等) |
| 换手率档位/异动 | 🟡 估值有,换手率档位需建 | 自建(小,数据已具备) |
| 筹码分布 + CAPITAL 信号 | ❌ 无 | **全新自建(最重、最不确定)** |
| 四类信号分级 B1-3/S1-3/ALERT/CAPITAL | 🟡 monitor.py 五哨兵(破位/回撤/放量下跌/估值透支/基本面恶化)≈ S 侧+ALERT 雏形 | 重做更细分级体系 |
| 大盘/板块环境过滤(RISK_ON/OFF) | ❌ 无显式 regime | **全新** |
| 止盈 + 盈亏比闸门 | ❌ 无 | **全新** |
| 持仓感知(cost_price/盈亏路由) | 🟡 有 watchlist,无成本价/路由 | 新增持仓模型 |
| 信号确认/冷静期状态机 | ❌ 无 | **全新(需状态持久化)** |
| 打分排序 −100~+100 | 🟡 有 screen rank_by 因子排序 | 重做综合打分 |
| 回测 | 🟡 向量化**单标的** backtest(无未来数据/T+1/真实成本/OOS) | **需事件驱动重写**(向量化不够,见 §2.3) |
| 告警推送 | ✅ 飞书 webhook(alert.py,urllib 无依赖) | **直接抄** |
| 仪表盘 | ✅ Streamlit 5 标签 | 重做(骨架可借鉴) |

**净结论:数据 / 告警 / 仪表盘骨架可借鉴或直抄;筹码 / regime / 止盈 / 确认状态机 / 事件驱动回测是全新重活,约占一半。**

---

## 2. 批判性评审:规范的洞 / 冲突 / 风险(按重要性排序)

### 2.1 【高】信号优先级冲突未定义
混沌行情里同一只票同日可能同时命中 B 侧和 S 侧条件。规范第 8 章的持仓路由(空仓主推 B、持有主推 S)**部分**化解了,但:
- 信号提醒(10.1)和打分(10.2)在两侧同时触发时**没有明确谁压谁**;
- 打分体系里同一现象**双计**:顶背离既进趋势分(−25)又触发 S1;放量滞涨既进量能分(−25)又是 S2 条件。这意味着"打分"和"信号"不是两个**独立**视角,而是部分同源——别把它们当成互相印证。
- **建议**:实现成**每只票每日一个状态机**输出单一主状态,而不是散落的 if 判断。优先级:`持仓状态 × 大盘状态` 共同决定主推哪一类。

### 2.2 【高】筹码分布 = 系统最大不确定性源
规范自己承认是估算值。`新分布 = 旧分布 ×(1−换手率)+ 当日新增` 是业界标准的"三角分布 + 换手衰减"模型(通达信/东财同款),但:
- 对换手率序列精度、起算窗口高度敏感;
- 主力对倒/对敲会虚增成交量 → 直接污染筹码估算;
- 复牌/大除权/次新股附近误差最大。
- **规范做对的地方**:筹码只占打分 20%、CAPITAL 信号定为中性、从不让筹码单独触发买卖。**保留这个克制。** 但"20% 权重压在一个估算量上"仍偏高,建议**回测里单独验证去掉筹码分后胜率是否真的下降**,不下降就降权。

### 2.3 【高】回测诚实性 = 隐藏的最大工作量(最易被偷工)
规范 11.3 说"上线前必回测",但**没说怎么回测**这套带次日确认、冷静期、regime 过滤、持仓路由的信号系统而**不引入未来函数**。难点:
- 次日确认 → 信号在生成上是 2 日前瞻,回测必须按**生效日 T+1** 计入,极易把日期标错导致结果虚高;
- 大盘 regime 过滤必须用 **as-of** 的 regime,不能用最终 regime;
- 持仓路由让回测**路径依赖**——你不能脱离模拟持仓独立评估每个信号。
- **这是向量化回测搞不定的,属于事件驱动回测范畴。** quant-pipeline 现有的是向量化单标的回测,**不够用**。
- **建议(对规范阶段顺序的修改之一)**:把一个**最小可用的事件驱动回测器提前到 P1 之后**就搭好,之后每加一个信号/阈值都用它验证,而不是等到最后。否则 P1–P5 全程的阈值都是拍脑袋,违背"不要用默认值实盘"。

### 2.4 【中】复权基准缝合跳变(实测过的坑)
只读复用 `daily_kline_v2`(到 06-05)+ tushare/akshare topup 到今天,**两段复权基准不同**,缝合点(06-05)附近若有除权会产生小跳变 → 直接污染 MA / 背离 / 筹码,可能在缝合日**凭空造出假信号**。
- **建议**:topup 后对缝合点做一次复权因子对齐校验;或对全序列用统一源重新前复权(代价是放弃本地加速)。仪表盘对缝合日附近信号加灰标。

### 2.5 ✅【已定】"实时"砍掉 —— 纯盘后分析
原规范标题写"实时",评审时指出数据层本就是 EOD。**用户 2026-06-27 拍板:不做实时/盘中,只做盘后分析。** 该顾虑消除,系统简化为:每交易日收盘后(15:05+)定时任务批算一次全自选股 → 出信号/打分/仪表盘。
- **连带简化**:无需接 QMT xtdata / akshare spot / 新浪盘口等实时源;数据层只剩 `daily_kline_v2` + 每日收盘后 topup 到当日。盘中刷新逻辑、分钟数据全部不做。
- 唯一的"新鲜度"机制 = 每日收盘后跑一次 topup,把本地库从 06-05 补到当天(见 §2.4 缝合校验)。

### 2.6 【中】阈值校准缺协议,且与"勿过拟合"原则张力
规范一边说"阈值回测校准、勿用默认值实盘",一边(设计原则 9)说"勿过度优化阈值、保留冗余优于历史完美拟合"——方向对,但**没给具体协议**。
- **建议**:用 **walk-forward / 样本外**校准,锁定后不再回看;明确"只调到能用,不调到最优"。quant-pipeline 的 `backtest_with_oos` 已有 OOS 思路可借鉴。

### 2.7 【中】板块 / 主力资金的数据可得性
- **板块强弱**:tushare 概念板块需高积分(你之前已确认,留人工);但**申万行业可行**——China_quant 有 `sw_l1_member`,可只读复用算行业等权指数当 `sector_strength`。概念板块降级为人工。
- **主力资金 `main_net_inflow`**:免费源不可靠,规范已预案(用换手+量价+筹码近似)。代价:CAPITAL(建仓/派发)信号证据最弱 → **保持中性提示,不入买卖主信号**(规范已如此)。

### 2.8 【低】状态机散落
第 9 章引入大量有状态机制(PENDING/CONFIRMED/FAILED、cooldown、expire、连续 2 次买入失败暂停、卖出后转强纠错)。规范把它们当独立规则列。
- **建议**:实现成**一个统一的、持久化的每股信号状态机**(一张 `signal_state` 表),而非散落 if。这是把第 9 章从"听起来很全"做成"实际不出 bug"的关键。

### 2.9 规范做得好、要保留的克制(别在实现时丢掉)
- 震荡指标(KDJ/RSI)只看背离、超买在多头排列下不当卖点、不扣分;
- 多指标共振、信号分三级而非非黑即白;
- 跟随而非预测、不追信号数量、保留冗余优于过拟合;
- 筹码/主力当中性提示不当主信号;
- 全程反未来函数、前复权、交易日历、板块阈值上浮。

---

## 3. 数据层真相(本轮已二次核对)

| 数据 | 来源 | 状态 |
|---|---|---|
| 个股连续前复权日线 + **换手率** + 涨跌幅 + peTTM/pbMRQ | `daily_kline_v2` | ✅ 到 **2026-06-05**(600519 实测 2530 行),**列名为正常 UTF-8 中文**(`日期/开盘/最高/最低/收盘/preclose/成交量/成交额/换手率/涨跌幅/ST标记/收盘_nfq/peTTM/pbMRQ`),引号列名直接查、零编码处理;需 topup 补到今天 |
| 最新横截面 / 估值 / 市值(circ_mv/total_mv) | `daily_snapshot` | ✅ 到 **2026-06-26**,但只是单日横截面 |
| 大盘指数日线(regime 用) | `index_daily_ext` / `index_daily_long` | ⚠️ 已核:本地只有 `000300.SH`(沪深300,到06-12)/`000852.SH`/`932000.CSI`,且**仅 close 一列**;**无 000001/399006/399001** → regime 指数走 tushare/akshare,或用本地**沪深300 当大盘代理** |
| 申万行业(板块强弱用) | `sw_l1_member`(China_quant) | ✅ 可只读复用 |
| 新鲜度 topup(补到今天) | tushare/akshare | ✅ quant-pipeline `_topup_local` 模式可抄 |
| 主力资金 net inflow | — | ❌ 无(符合规范预期,用换手+量价+筹码近似) |
| 概念板块 | tushare | ⚠️ 需高积分,降级人工 |

**关键正面结论**:筹码估算所需的换手率序列、大盘 regime 所需指数、板块所需申万行业,**本地全部拿得到**。唯一硬缺口是主力资金,规范已有预案。

---

## 4. 落地方案(独立新仓)

### 4.1 推荐仓库结构(沿用 quant-pipeline 积木哲学:算/画分离、配置中心、严禁未来函数)
```
stock-monitor/                 # 仓名待你定
  smon/
    config.py      # YAML 配置中心(规范 11.4 结构)+ 密钥读取
    sources.py     # 只读复用 daily_kline_v2(中文列名映射)+ topup + 指数 + 申万行业
    indicators.py  # MA/MACD/KDJ/RSI/ATR/BOLL —— 只算不画
    turnover.py    # 换手率档位/异动/量价配合
    chips.py       # 筹码分布估算(三角分布+换手衰减)—— P4
    market.py      # 大盘/板块 regime(RISK_ON/NEUTRAL/RISK_OFF + sector_strength)
    signals.py     # 四类信号分级 B/S/ALERT/CAPITAL(多指标共振)
    takeprofit.py  # 止盈 + 盈亏比闸门(买入前置过滤)
    position.py    # 持仓状态 + 盈亏路由(个性化输出)
    state.py       # ★ 统一信号状态机(PENDING/CONFIRMED/FAILED/cooldown/expire)持久化
    score.py       # −100~+100 综合打分 + 环境调整
    pipeline.py    # 编排:先大盘→再个股→信号→环境过滤→输出
    alert.py       # 飞书/邮件推送(抄 quant-pipeline)
    store.py       # SQLite:日线缓存 + 信号历史 + 持仓 + signal_state
    backtest.py    # ★ 事件驱动回测(as-of regime + 次日确认 + 持仓路径依赖)
  run.py           # click CLI:run / score / signal / backtest / alert / update
  app.py           # Streamlit:三输出(提醒/打分/仪表盘)
  config.yaml      # 配置中心
  tests/
```
**继承铁律**:indicators 只算不画;严禁未来函数;前复权;走交易日历;科创/创业板阈值上浮 30–50%。

### 4.2 分阶段(我对规范阶段顺序做了两处调整,理由见 §2.3 / §2.5)

| 阶段 | 内容 | 对应规范 | 备注 |
|---|---|---|---|
| **P0** 数据底座 | sources 只读复用 + topup 到今天 + 指数/申万取数 + 字段可得性核验 | 11.1 | 先把中文列名映射、缝合跳变(§2.4)处理掉 |
| **P1** 指标+打分+基础 B/S 三级 | MA/MACD/KDJ/RSI/ATR/BOLL + 打分排序 + 基础买卖三级(无筹码/无 regime) | 阶段1 | CLI/Streamlit 出打分 |
| **P1.5** ★最小事件驱动回测器 | 提前搭好,之后每加信号都验 | (规范未明确) | **§2.3 的关键修改** |
| **P2** 量价细分 + ATR 吊灯止损 + 告警 | 放量滞涨/量价配合 + 吊灯止损(只上移)+ 飞书推送 | 阶段2 | 告警抄 quant-pipeline |
| **P3** ★大盘/板块过滤 + 止盈/盈亏比 | regime 三档 + 信号升降级 + 盈亏比闸门 + 移动止盈 | 第6/8章 | **提到筹码之前**(规范也认同第6章最该优先,且不依赖筹码) |
| **P3.5** ★多周期+钝化调权(v2.1) | 周线背景常驻(日线合成,因果)+ 日线钝化检测 + 钝化时切"趋势主导"动态调权 | 第7章(v2.1) | 直击 P1.5 暴露的趋势股 whipsaw;周线只上 MA/MACD(规范§7.5) |
| **P4** 筹码分布 + CAPITAL | 三角分布+衰减估算 + 筹码信号 + 仪表盘筹码图 | 阶段3 | 最重、最不确定,放后面 |
| **P5** 持仓感知 + 信号确认状态机 | cost_price 盈亏路由 + 统一状态机 + 冷静期 | 第9/10章 | state.py 一张表收口 |
| **P6** 回测校准 + 每日定时任务 | walk-forward 校准阈值 + 收盘后 15:05 定时跑全流程(schtasks) | 阶段7 | ~~盘中准实时~~ 已砍 |

**对规范阶段顺序的修改**:
1. **回测器从"最后"提到 P1.5**——否则全程阈值靠拍脑袋。
2. **大盘/板块过滤(规范阶段4)提到筹码(阶段3)之前**——规范 11.2 自己也说"环境过滤是 v2.0 最该优先补的",且它不依赖筹码,先做收益更高、风险更低。
3. **(v2.1)多周期+钝化调权设为 P3.5**——规范把它放第六阶段,但它直击 P1.5 回测暴露的"趋势股被甩下车"问题(周线多头+未破生命线时不被日线噪音震出),且与 P3 的大盘过滤天然叠成"大盘→周线→日线"三层过滤。可视回测结果决定是否再提前。

### 4.2.1 v2.1 增量评审(第7章 多周期+钝化)
- **最值钱的洞**:"钝化时换的是**指标类型**(回到趋势类),不是周期"——周线 KDJ 在大趋势里同样钝化,这句纠正了"钝化就上周线"的常见错误。规范 §7.5 也克制地只让 MA/MACD 上周线,KDJ 辅助,ATR/量/筹码不上,避免冗余。✅ 保留这份克制。
- **⚠️ 最大实现坑——周线必须因果合成**:规范要"每日用截至当日的**不完整本周** K 线"更新周线。回测时,历史第 T 日的周线背景只能用 ≤T 的日线合成的"周内到 T 为止"K 线,**严禁**用 resample 出的"完整本周"(那是未来函数)。做法:把周线指标对齐回日线索引、每日取"本周已发生部分 + 历史完整周"。这条若写错,P3.5 回测会虚高,必须按 P1.5 的截断重算法二次验证。
- **钝化检测是启发式**:"≥2次金叉死叉来回""J持续>90"等阈值规范自己也说是经验值 → 上线前用 backtest 验证它确实改善(尤其在我们这 8 只趋势股上),别假设。
- **打分受影响**:score.py 要加 v2.1 调整(周线多头正分×1.1/空头正分×0.7负分×1.2;钝化时日线 KDJ/RSI 的±5 归零、趋势权重提高)。这会与 P3 的大盘×系数叠加,注意乘法链顺序与可解释性。

### 4.3 复制 vs 新建边界(独立程度的落地)
**原则:fork(把 quant-pipeline 代码搬进 `feifei_Ashare/smon/` 改),不 import。运行时零跨仓依赖。**

| 直接搬过来改(已被多智能体审阅修过 bug,别重造) | 全新建(quant-pipeline 没有或不够用) |
|---|---|
| 数据层:`daily_kline_v2` UTF-8 中文列名(引号查)+ `_topup_local` + 交易日历 | `indicators.py` 完整 MA/MACD/KDJ/RSI/ATR/BOLL 衍生字段 |
| `alert.py` 飞书 webhook(urllib 零依赖) | `signals.py` 四类信号分级 B/S/ALERT/CAPITAL |
| `config.py` YAML 配置中心 + `_load_secret` | `market.py` 大盘/板块 regime |
| `costs.py` 真实费率成本模型(回测要用) | `chips.py` 筹码分布估算 |
| `store.py` SQLite 骨架 | `takeprofit.py` 止盈/盈亏比闸门 |
| `app.py` Streamlit 多标签骨架 | `position.py` 持仓路由 + `state.py` 统一状态机 |
| | `score.py` 综合打分 + `backtest.py` 事件驱动回测 |

**代价(已知并接受)**:搬过来的是**快照**;quant-pipeline 日后修了数据层 bug,feifei 不自动同步,需手动搬。换运行时彻底独立,值。
**不变的边界**:feifei 只**读** China_quant 的 `stock_data.db`,**绝不写**;只**复制** quant-pipeline 的代码,**绝不 import/反向依赖**。

---

## 5. 决策状态

| # | 决策 | 状态 |
|---|---|---|
| 1 | 仓库名 + 路径 | ✅ **`feifei_Ashare`** @ `C:\Users\Administrator\feifei_Ashare` |
| 2 | 阶段顺序调整(回测器提前 P1.5 / 环境过滤提到筹码前) | ✅ 同意 |
| 3 | 独立程度 | ✅ **运行时 100% 独立(零跨仓依赖)+ 开发上 fork 已验证模块**(见 §4.3) |
| 4 | 告警渠道 | ⬜ 待定(建议飞书:你已有 webhook 基建,直接搬 alert.py) |
| 5 | 自选股初始名单 watchlist | ⬜ 待定(规范示例 300395/688256/300308;非 P0 阻塞,先放占位) |

> #4、#5 不阻塞 P0/P1,可后补;P0 先用占位名单 + 不接告警即可跑通。

## 6. Build 阶段待核实清单
- [x] ~~`daily_kline_v2` 列名~~ → 已核:正常 UTF-8 中文,引号列名直接查(2026-06-27)
- [x] ~~`index_daily_ext/long` 是否含 000001/399006~~ → 已核:**无**,本地仅沪深300/中证1000/中证2000 且 close-only;regime 指数走 tushare/akshare 或沪深300 代理
- [ ] topup 缝合点(06-05)复权因子对齐校验(P0 sources.py 处理)
- [ ] `sw_l1_member` 当前结构是否够算行业等权指数
- [ ] 科创/创业板换手率、涨跌幅阈值上浮系数标定
