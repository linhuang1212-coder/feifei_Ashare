# feifei_Ashare 使用说明

A股自选股 + 全市场**盘后监测/提醒**工具。它每天收盘后基于价量数据,给你三类东西:**信号提醒、综合打分、可视仪表盘**,帮你看盘做决策。

> ⚠️ **它不是自动交易、也不替你下单**。所有信号/打分都是技术指标的客观计算,有滞后和失效可能,**只作参考,买卖自己判断、风险自负**。

---

## 〇、最简单的用法(每天 3 步)

1. **收盘后更新数据**:工作日 18:00 计划任务会自动跑(见第五节);想手动就双击或命令行 `py run.py daily`。
2. **打开仪表盘看盘**:浏览器开 **http://localhost:8602**(没开的话先启动,见第二节)。
3. **重点看**:顶部大盘环境灯 → 自选总览的"综合分/信号/你的动作" → 想深看就进单股详情。

---

## 一、它能回答你什么

- 今天大盘是进攻还是防守?(顶部红/灰/绿环境灯)
- 我的自选股今天谁最该关注?(综合分排序)
- 这只票现在是买点还是卖点?到第几级?(B1–B3 买 / S1–S3 卖)
- 我持有的票该减还是该拿?(持仓动作:止盈减仓/持有观察/清仓)
- 全市场今天谁最强?(全市场排行扫描)
- 某只票的指标到底满足了哪些条件?(指标达标清单,自己逐项核对)

---

## 二、打开/启动仪表盘(网站)

仪表盘地址:**http://localhost:8602**(本机浏览器打开)。

如果打不开,说明服务没在跑,启动它(命令行):
```
cd C:\Users\Administrator\feifei_Ashare
py -m streamlit run app.py
```
> 本机 `py` 指向 `C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe`。
> 默认端口可能是 8501;我们常用 8602,以浏览器实际弹出的地址为准。

---

## 三、仪表盘三个视图(左侧切换)

### 📊 自选总览
你的自选股(在 `config.yaml` 里设)一张表,按综合分从高到低排。每列含义:
- **综合分**:−100~+100。**红=偏多、绿=偏空、灰=中性**。档位:强多>50 / 偏多20~50 / 中性±20 / 偏空-50~-20 / 强空<-50。
- **趋势/量能/位置**:综合分的三个分项(还有筹码项)。
- **周线背景**:中期方向(周线多头/空头/中性)。
- **持仓**:你在 config 标了持仓的票,显示持有±盈亏%。
- **信号**:🔴Bx买 / 🟢Sx卖。

### 🔍 单股详情(可搜全市场 5500+ 只)
下拉框**输代码或名字搜任意一只**(不限自选)。一张状态卡:
- 顶部:现价、综合分、周线背景、120日位置分位、当前信号、**所属板块及强弱**。
- **信号区**:当前买卖信号 + 理由;⏳待确认 / ❄冷静期 提示。
- **给你的动作**(若标了持仓):空仓→看买点;持有→看止盈/止损;并显示盈亏、移动止盈线、止盈分级。
- **趋势/量能位置/震荡区**:均线、MACD、生命线、换手、量比、KDJ/RSI(标"仅辅助")、背离、钝化提示。
- **🎯 筹码分布**:获利盘比例、平均成本、集中度、主力动向(估)+ 横向筹码图(标现价/成本线)。
- **K线图**:近120日 K线 + 均线 + ATR吊灯止损线。
- **📋 指标达标清单**:六组逐项 ✅满足 / ⬜未满足 / ▸数值 —— 你想"自己过一遍"就看这里。

### 🔥 全市场排行
点按钮跑**全市场扫描**(约 2–3 分钟,结果会缓存)。出强多榜首(或勾"强空垫底")。看中的票可加进自选(改 config)。

---

## 四、关键概念怎么看懂

| 名词 | 含义 |
|---|---|
| **大盘环境(顶部灯)** | RISK_ON进攻/NEUTRAL中性/RISK_OFF防守。防守时买入信号自动暂停、卖出升级、打分打折——别逆大盘。 |
| **B1/B2/B3** | 买入三级:B1关注、B2建仓、B3强烈。**B2及以上才比较值得看**。 |
| **S1/S2/S3** | 卖出三级:S1预警(先别动)、S2减仓1/3~1/2、S3清仓/留底仓。 |
| **盈亏比** | 买入信号旁的数字,上方空间/止损风险。<1.5 的买入已被自动丢弃。 |
| **⏳待确认** | 信号今天刚出、还没持续到第二天。稳健者等次日确认再动。 |
| **❄冷静期** | 该票刚触止损,几天内买入信号被抑制,防止反复打脸。 |
| **周线背景** | 中期方向。周线多头时日线回调容忍度高;周线空头要谨慎。 |
| **板块强弱** | 个股所属申万行业近20日在全市场的排名分位。强板块买入加分,弱板块卖出强化。 |
| **筹码/获利盘** | 估算值,看套牢/获利分布与成本区,仅供参考。 |

> 核心原则:**任何买卖都要趋势+量能+位置等多个条件共振**,不靠单一指标;震荡指标(KDJ/RSI)只看背离、强趋势里会"钝化"失灵(系统会自动降权并提示)。

---

## 五、命令行(不开仪表盘也能用)

在 `C:\Users\Administrator\feifei_Ashare` 下:
```
py run.py score              # 自选股打分排序 + 信号 + 大盘环境 + 你的持仓动作
py run.py check 600519       # 某只票的"指标达标清单"(不填代码=全部自选)
py run.py screen --top 30    # 全市场扫描强多榜首(加 --short 看强空)
py run.py backtest 600519    # 某只票信号策略的历史回测(体检用,非保证收益)
py run.py update             # 全市场数据补鲜到最新交易日(收盘后)
py run.py daily              # 收盘后一条龙:补鲜 + 打分 + 存信号
py run.py cache              # 首次/数据异常时重建全市场缓存
```

---

## 六、收盘后定时任务(自动每天跑)

让它工作日 18:00 自动 `update + score`:
1. **右键 `register_task.bat` →「以管理员身份运行」**(计划任务需要管理员权限)。
2. 成功后:工作日 18:00 自动跑 `daily.bat`,日志在 `logs\daily.log`。
3. 相关命令(管理员窗口):
   - 手动测一次:`schtasks /Run /TN feifei-daily`
   - 查看:`schtasks /Query /TN feifei-daily`
   - 删除:`schtasks /Delete /TN feifei-daily /F`

> 注:默认"仅当前用户登录时运行"。要关机/不登录也跑,需在任务计划程序里改成"不管用户是否登录"。
> 数据跑完后,刷新浏览器即可看到当天最新盘面。

---

## 七、改自选股 / 标持仓

编辑 `config.yaml` 的 `watchlist`:
```yaml
watchlist:
  - { code: "600519", name: "贵州茅台" }                          # 空仓观察
  - { code: "300750", name: "宁德时代", position_status: HOLDING, cost_price: 180, entry_date: "2026-03-01" }
  - { code: "002594", name: "比亚迪", position_status: WATCHING }
```
- `position_status`:`EMPTY`空仓 / `HOLDING`持有 / `WATCHING`观察。
- 持有要填 `cost_price`(成本价),系统才能算你的盈亏、给个性化动作;`entry_date`(建仓日)用于算移动止盈。
- 改完刷新仪表盘即可(改了名单不用重建缓存)。

---

## 八、注意事项

- **周末/节假日**:A股休市,数据停在最后交易日(如周五),这是正常的,不是 bug。下个交易日收盘后才有新数据。
- **数据新鲜度**:仪表盘顶部显示"数据截至 X 月 X 日"。如果不是最新交易日,跑一次 `py run.py update`。
- **改了代码后**:若改的是 `smon\` 下的程序文件,仪表盘要**重启**才生效(关掉重新 `streamlit run`)。改 `config.yaml` 不用重启,刷新即可。
- **tushare token**:数据补鲜用,放在 `secrets.local.yaml`(不会进 Git)。它曾在聊天里明文出现,建议去 tushare 后台 rotate 一次。
- **全市场排行**首次点要等 2–3 分钟(全市场粗筛),之后缓存,切走再回不重跑。

---

## 九、免责声明

本工具输出的所有信号、评分、状态,仅为基于历史价量数据的**客观计算结果**,**不构成任何投资建议、买卖指令或收益承诺**。技术指标有滞后性,任何信号都可能失效;筹码分布为估算值。实盘前请自行回测校准,交易后果自负。股市有风险,投资需谨慎。
