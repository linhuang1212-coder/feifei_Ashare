# P0 数据底座
pandas
numpy
pyyaml
loguru
akshare        # 免 token 的缺口补源 / 回退源
tushare        # 可选:首选缺口补源,需 token(secrets.local.yaml 或 env TUSHARE_TOKEN)

streamlit      # 仪表盘
plotly         # K线/指标图

# 后续阶段(用到再装):
# pandas-ta     # (本项目指标为手写,未用)
